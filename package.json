{
  "name": "regional-t",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend/client",
    "api"
  ],
  "scripts": {
    "dev": "npm run dev --prefix frontend/client",
    "build": "npm run build --prefix frontend/client",
    "lint": "npm run lint --prefix frontend/client",
    "test": "npm run test --prefix frontend/client && npm run test --prefix api",
    "test:frontend": "npm run test --prefix frontend/client",
    "test:api": "npm run test --prefix api"
  }
}
